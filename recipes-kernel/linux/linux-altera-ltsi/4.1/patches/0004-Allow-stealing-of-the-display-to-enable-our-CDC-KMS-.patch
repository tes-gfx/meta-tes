From c9307e4f17bb6a0676a05acd2dfec068f4ec0cc5 Mon Sep 17 00:00:00 2001
From: Christian Thaler <christian.thaler@tes-dst.com>
Date: Fri, 8 Jul 2016 10:24:50 +0200
Subject: [PATCH] Allow stealing of the display to enable our CDC KMS hack in
 the Qt fbdev plugin.

---
 drivers/gpu/drm/drm_fb_helper.c | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/drivers/gpu/drm/drm_fb_helper.c b/drivers/gpu/drm/drm_fb_helper.c
index cac4229..3ac5fa2 100644
--- a/drivers/gpu/drm/drm_fb_helper.c
+++ b/drivers/gpu/drm/drm_fb_helper.c
@@ -455,8 +455,8 @@ static bool drm_fb_helper_is_bound(struct drm_fb_helper *fb_helper)
 
 	/* Sometimes user space wants everything disabled, so don't steal the
 	 * display if there's a master. */
-	if (dev->primary->master)
-		return false;
+//	if (dev->primary->master)
+//		return false;
 
 	list_for_each_entry(crtc, &dev->mode_config.crtc_list, head) {
 		if (crtc->primary->fb)
-- 
1.9.1

